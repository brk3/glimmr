<?xml version="1.0" encoding="utf-8"?>

<!-- Fragments will clear the menu and add these items, but for activities
     without fragments these base items should always appear -->

<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/menu_search"
        android:actionViewClass="android.widget.SearchView"
        android:icon="@drawable/ic_action_search_dark"
        android:showAsAction="ifRoom|collapseActionView"
        android:title="@string/menu_search"/>

    <item android:id="@+id/menu_preferences"
        android:icon="@android:drawable/ic_menu_preferences"
        android:showAsAction="never"
        android:title="@string/menu_preferences" />

    <item android:id="@+id/menu_about"
        android:icon="@drawable/ic_action_about_dark"
        android:showAsAction="never"
        android:title="@string/about" />
</menu>
